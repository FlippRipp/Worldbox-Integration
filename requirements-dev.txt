# Test/dev dependencies. The module itself has no install requirements beyond
# what WorldboxAI already ships (httpx, websockets).
pytest>=8.0
pytest-asyncio>=0.24
httpx>=0.27
websockets>=12.0
fastapi>=0.115
